import React from "react";
import Lines from "./Lines";

const Hero = () => {
  return (
    <div className="relative h-screen grid grid-rows-6 gap">
      <Lines />
    </div>
  );
};

export default Hero;
